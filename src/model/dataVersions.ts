import versions from './data-versions.json';

export const BACKUP_VERSION = versions.backup;
export const DAILY_ENTRY_SCHEMA_VERSION = versions.dailyEntry;
export const SETTINGS_VERSION = versions.settings;
export const AI_REPORT_VERSION = versions.aiReport;
export const FIRST_USE_FUNNEL_VERSION = versions.firstUseFunnel;
export const INDEXED_DB_VERSION = versions.indexedDb;

export const MIN_SUPPORTED_BACKUP_VERSION = 1;
