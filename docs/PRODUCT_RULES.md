# Правила развития «Траектории»

Эта памятка нужна, чтобы приложение оставалось инструментом для фактов, решений и действий, а не превращалось в перегруженный дневник, систему самоосуждения или коллекцию красивых, но бесполезных метрик.

## Назначение

«Траектория» — это зеркало жизни на уровне наблюдаемых фактов:

- что я делал;
- как я спал и восстанавливался;
- что мешало;
- что помогало;
- какие события меняли контекст;
- какие завершённые итоги реально появились.

Цель приложения — снижать искажения памяти, показывать то, что повторяется, и помогать выбрать одно-два практических изменения на следующий период.

## Главный фильтр нового поля

Новое поле стоит добавлять только если оно проходит все проверки:

- его можно честно заполнить вручную без большого трения;
- оно помогает принять решение на уровне недели, месяца или долгого периода;
- оно не создаёт иллюзию точности там, где данных мало;
- оно не дублирует уже существующее поле;
- оно не превращает ежедневную запись в тяжёлую анкету;
- оно может появиться в сводке, трендах или экспортном JSON;
- оно помогает выбрать действие, а не просто удовлетворяет любопытство.

Жёсткое правило: если поле не помогает недельному или месячному разбору, его не нужно добавлять в основную ежедневную запись.

## Защита направления продукта

Новая идея не становится правильным направлением только потому, что она интересна или получила предварительное согласие.

Перед серьёзной доработкой нужно проверить:

- какую подтверждённую проблему она решает;
- как часто возникает эта проблема;
- какое решение пользователя станет лучше;
- не противоречит ли функция назначению приложения;
- можно ли получить основную пользу меньшим изменением;
- какие постоянные расходы она добавит в данные, интерфейс, миграции, тесты и поддержку;
- по какому наблюдаемому признаку после внедрения станет понятно, что решение полезно.

Если проблема единична, основана только на предположении или требует непропорциональной сложности, функция не переходит сразу в разработку. Она откладывается как гипотеза, проверяется узким прототипом либо отклоняется. Агент обязан явно сообщать такие сомнения и рекомендовать более простую границу, даже если первоначальная идея принадлежит владельцу проекта.

## Уровни данных

День: минимум фактов, заполнение примерно за минуту.

Неделя: повторяющиеся факторы, связь сна, энергии, действий и помех.

Месяц: итоги, важные события, повторяющиеся условия и заметные изменения.

3, 6 и 12 месяцев: долгие изменения, сезонность, крупные решения, повторяющиеся провалы и рабочие стратегии.

## Сменяемые блоки записи

Сон, карьера, физическая активность и питание могут быть временно скрыты из ежедневной формы, если они больше не относятся к текущим задачам пользователя.

- скрытие блока не удаляет и не обнуляет уже сохранённые значения;
- история продолжает участвовать в обзорах, трендах и выгрузке;
- скрытый блок не должен мешать сохранению остальных частей старой записи;
- общие факты, необычные дни и события остаются доступны независимо от выбранных блоков;
- изменение состава блоков само по себе не делает данные до и после полностью сопоставимыми.
- системные варианты физической активности остаются общими; личные занятия добавляются в настройках, могут быть убраны из нового ввода и позже возвращены без удаления прежних отметок;

Будущий конструктор следует ограничениям из `docs/BLOCK_CONSTRUCTOR.md`. Свободный редактор произвольных форм не допускается: блок создаётся из шаблона, тип поля заранее определяет допустимое хранение и отображение, а изменение смысла или единицы создаёт новую версию данных.

## Научная осторожность

- Корреляция не равна причине.
- Малое количество дней не даёт надёжных выводов.
- Субъективные оценки полезны как личные тренды, но не как объективная диагностика.
- Особые дни нужно учитывать отдельно, иначе они искажают средние значения.
- Приложение не должно делать медицинские выводы или заменять специалиста.
- Нельзя считать общий балл жизни, успешности или ценности дня.
- Лучше показывать диапазоны, частоты и повторяющиеся условия, чем точные псевдоформулы.

## Статистическая честность

- У среднего значения всегда должно быть видно число наблюдений понятными словами, без необъяснённых статистических сокращений.
- Доля считается только от дней, где соответствующий показатель действительно отмечен.
- Явная отметка «ничего не было» отличается от пропущенного блока; пропуск нельзя автоматически считать нулём.
- Особые дни не входят в базовые средние сна, энергии, качества сна и веса, но остаются видимыми отдельно.
- Текущий неполный месяц должен быть явно помечен и не сравниваться с полным как равный ему период.
- Длинные периоды используют доли и устойчивость по месяцам, а не пороги одного месяца.
- Фактор сравнивается с обычными днями без него; одного среднего в дни с фактором недостаточно.
- При малом числе наблюдений показывается нехватка данных, а не сильный вывод.
- Автоматическое сравнение двух групп требует хотя бы четырёх наблюдений в каждой; это минимальный фильтр от случайного шума, а не гарантия вывода.
- Пропуски не считаются нулём и не должны незаметно соединяться на графике.
- Календарная ось сохраняет пропущенные даты, чтобы редкие записи не выглядели регулярными.
- Карта заполнения показывает надёжность данных, но не серии, дисциплину или качество человека.
- При сравнении события используются равные календарные окна до и после; сам день события исключается.

### Аналитический контракт ежедневной записи

Достоверность начинается не с графика, а с однозначного смысла сохранённого ответа.

- Для каждого анализируемого поля хранится факт того, что пользователь действительно его отметил. Наличие пустой записи за дату или видимость блока не считаются ответом.
- Пустой отмеченный список означает «ничего из вариантов не было»; неотмеченное поле означает пропуск. Эти состояния имеют разные знаменатели и не объединяются.
- Новая запись сохраняет версию своей схемы и снимок показанных сменяемых блоков. Изменение настроек позже не переписывает исторический контекст.
- Для старой записи неизвестный состав блоков остаётся неизвестным. Миграция может восстановить только те отметки, которые подтверждаются значением или прежним явным флагом, и не должна подставлять текущие настройки задним числом.
- Изменение определения, единицы, диапазона шкалы или момента измерения создаёт новую версию ряда данных.
- Автоматическая аналитика использует в знаменателе только записи, где соответствующее поле отмечено. Количество таких записей показывается рядом с результатом.

Полнота данных оценивается относительно вопроса. Текущая ежедневная запись достаточна для описания фактов и повторяющихся связей, но сама по себе не доказывает влияние и не позволяет прогнозировать жизненный результат.

Техническая семантика состояний, версий и знаменателей зафиксирована в `docs/ANALYTICS_DATA_CONTRACT.md`.
- Сравнение до и после события показывает временную связь, а не доказывает его эффект.
- Решения из обзоров датируются фактическим сохранением; для старых записей без времени используется конец недели или месяца.

## Антипаттерны

В приложение не стоит добавлять:

- общий score дня;
- штрафы, shame-механику и агрессивные streaks;
- медицинские диагнозы и рекомендации;
- фазы сна без надёжного источника данных;
- выводы о причинности на малой выборке;
- обязательные поля «на всякий случай»;
- многоуровневые анкеты в ежедневной записи;
- моральные оценки поведения;
- мотивационные лозунги вместо фактов;
- сравнение себя с нормой без контекста.

## Сон и состояние

Текущая модель хороша, потому что разделяет несколько разных вещей:

- сколько примерно было сна;
- время отхода ко сну и подъёма;
- сколько времени было проведено в кровати;
- субъективное качество сна;
- энергия днём.

Это лучше, чем одно поле «сон», потому что долго лежать в кровати и реально спать — разные состояния.

Позже можно добавить только простой признак ночных пробуждений, если он будет влиять на решения периода и не утяжелит запись.

Временная привязка записи:

- запись за выбранную дату описывает ночь перед этой датой;
- энергия относится к дню после неё.

Что не стоит добавлять без внешних данных:

- точные фазы сна;
- REM/deep sleep;
- сложные индексы восстановления;
- медицинскую интерпретацию бессонницы.

## Контекст дня

Контекст — отдельный необязательный блок, который не принадлежит сну, карьере или текущей цели. Он содержит повторяющиеся условия, свободное пояснение и отметку необычного дня.

Этот блок должен описывать условия, а не обвинять или заранее объявлять их причиной.

Подходящие факторы:

- изменившийся режим сна;
- экранное время;
- новости и информационная нагрузка;
- видео или сериалы;
- рабочая нагрузка;
- поздний приём пищи;
- кофеин или алкоголь;
- эмоциональная нагрузка;
- пользовательское условие, которое имеет устойчивый и понятный ему смысл.

Формулировка должна быть нейтральной: не «мешающий фактор» и не «плохое поведение», а «условие, которое могло быть связано с состоянием или ходом дня».

Системный список остаётся коротким и нейтральным. Любой системный или пользовательский фактор можно скрыть из нового ввода и позже вернуть; подпись уже использованного фактора сохраняется в истории. Скрытие любого фокуса не скрывает контекст. Сам контекст можно отключить отдельно без удаления исторических данных.

«Восстановление» не является направлением действия по текущей цели: оно уже отмечается как активность или особый день. Старые записи с таким направлением сохраняются для совместимости, но вариант не предлагается в новом вводе.

## Карьера

Карьера должна отслеживать действия, которые могут дать ответ извне:

- отклики;
- сообщения;
- созвоны;
- собеседования;
- тестовые;
- офферы;
- отказы;
- паузы.

Не нужно превращать раздел в оценку личности, продуктивности или «достаточно ли я старался».

## Действия по текущей цели

Этот блок помогает различать конкретные действия по цели, подготовку и дни, занятые другими делами.

Текущая цель может иметь наблюдаемый результат и дату пересмотра. Дата пересмотра — это момент проверки решения, а не обещание достичь результата к сроку. Новые дневные записи сохраняют снимок этих определений, чтобы изменение настроек не переписывало смысл истории.

Хороший формат:

- конкретное действие — то, после чего мог появиться заметный результат или обратная связь;
- подготовка — обучение, настройка, планирование, улучшение базы;
- поддержание — действие, которое сохраняет режим или устойчивость;
- занимался другим — день в основном был посвящён не выбранной цели.

Подготовка не считается ошибкой. Она становится сигналом для проверки только тогда, когда долго не приводит к конкретным действиям.

Главный вопрос периода: какие действия привели к заметному результату или обратной связи?

Плохой формат:

- считать подготовку провалом;
- стыдить за дни, занятые другими делами;
- считать любое обучение избеганием;
- делать вывод о цели по одному дню;
- заменять конкретное действие красивой формулировкой намерения.

## Движение

Движение лучше хранить как факт и тип активности, а не как точный расчёт пользы.

Хорошие признаки:

- была ли активность;
- какая именно;
- насколько регулярно она появляется;
- как она связана с энергией и сном.

Плохие признаки для ручного трекинга:

- калории без устройства;
- точная нагрузка без данных;
- сложные тренировочные индексы.

## Питание и вес

Питание нужно отслеживать как условие, которое поддерживало или мешало цели, а не как моральную оценку дня.

Хороший формат:

- поддержало цель;
- нейтрально;
- мешало цели;
- короткий контекст;
- опциональный вес.

Вес стоит смотреть по неделям и месяцам, а не по одному дню. Разовые колебания веса не должны превращаться в выводы о прогрессе или провале.

Критерий «поддержало цель» задаётся заранее через наблюдаемые признаки. При его изменении старые записи сохраняют прежний критерий, чтобы история не переписывалась задним числом.

Плохой формат:

- стыд за еду;
- жёсткая оценка «сорвался» как основной язык интерфейса;
- ежедневный общий балл питания;
- выводы по весу за один день;
- сложный подсчёт калорий без готовности реально его вести.

## Области жизни

Области жизни нужны, чтобы видеть присутствие важных направлений, а не оценивать себя по всем фронтам.

Хороший формат:

- семья;
- чтение;
- творчество;
- личная практика с конкретным и понятным названием;
- отдых;
- друзья;
- изучаемый язык или другой личный пункт, добавленный пользователем;
- другая пользовательская область.

Плохой формат:

- десятибалльная оценка каждой области каждый день;
- обязательный комментарий к каждой области;
- попытка сделать идеальную карту всей жизни.

## Важные события

Важные события нужны для контекста, который объясняет тренды:

- переезд;
- болезнь;
- смена режима;
- начало или конец проекта;
- важное решение;
- конфликт;
- поездка;
- крупная покупка;
- изменение работы;
- терапия, спорт, обучение или другая новая практика.

События не должны заменять дневник. Их задача — дать объясняющий слой для недель, месяцев и долгих периодов.

Важные события должны быть видимы на временной шкале показателей. Для выбранного события приложение может сравнивать сон, энергию, вес, действия, питание и итоги в равных окнах до и после, обязательно показывая полноту данных и `n`.

Инсайт хранится в этом же разделе как отдельный тип записи. Это мысль или понимание, которое может повлиять на решение или позже помочь объяснить изменения. Инсайт не нужно оформлять как завершённый итог и нельзя автоматически считать доказанной причиной того, что повторяется в данных.

Инсайт может быть длинной цельной мыслью. Пользователь не обязан заранее разделять её на темы ради структуры приложения. Поле заметки растёт во время ввода, а длинный текст в архиве сначала показывается компактным превью и раскрывается по запросу. Метки и темы, если появятся позже, остаются необязательными.

## Итоги

Итоги — это завершённые и отдельно учитываемые факты, а не список задач и не любое наблюдение дня.

Факт дня сохраняет событие, изменение или контекст, который важно помнить. Один и тот же текст не стоит автоматически дублировать в итоги: туда он попадает только если обозначает завершённое действие, полученный ответ или созданную вещь.

Короткая отметка в ежедневном блоке и развёрнутый итог могут относиться к одному действию, но выполняют разные задачи. Дневная отметка отвечает «было ли это сегодня?» и подходит для подсчёта регулярности. Итог отвечает «что именно завершено или получено?» и сохраняет содержательный результат. Приложение не должно автоматически объединять или удалять такие записи как дубли.

Хорошо:

- завершил проект;
- отправил N откликов;
- прошёл собеседование;
- дочитал книгу;
- сделал релиз;
- выдержал эксперимент.

Плохо:

- «надо сделать»;
- незавершённые планы;
- размытые обещания себе.

## Тренды

Тренды должны помогать увидеть направление, а не доказывать красивую теорию.

Правильные вопросы:

- что повторяется последние 3 месяца;
- какие факторы часто совпадают с низкой энергией;
- какие периоды были сильнее и чем они отличались;
- какие важные события могли изменить динамику;
- какие маленькие действия стабильно возвращаются в хорошие недели.

Неправильные вопросы:

- «почему я такой»;
- «какая одна причина всего»;
- «какой точный прогноз моей жизни».

На графиках трендов:

- действия и питание сравниваются как доли отмеченных дней;
- рядом со средними показывается число измерений;
- вес отделяется от сна и энергии;
- сглаживание не должно придумывать значения между месяцами;
- важные события отображаются как контекстные маркеры;
- решения недель и месяцев, их последующая проверка и завершённые итоги собираются в одну хронологию.
- текущий период обрывается на сегодняшней дате; сглаженная линия не продолжается в будущее и не выглядит как прогноз.

## Длинные списки и архивы

Итоги, события и другие растущие коллекции не должны бесконечно удлинять основной экран.

- архивные страницы дают поиск, диапазон дат и фильтр по типу;
- сначала показывается ограниченная порция записей с явной кнопкой продолжения;
- в недельных и месячных сводках длинный список сворачивается до нескольких элементов;
- вложенная прокрутка внутри карточки не используется как основная навигация по истории;
- пользовательские пункты, уже встречавшиеся в записях, архивируются, а не удаляются вместе с подписью истории.

## Эксперимент

Эксперимент должен разделять соблюдение условия и результат. Обязательная логика:

- одно изменяемое условие;
- даты начала и окончания;
- ежедневная отметка соблюдения;
- собственный текстовый вывод пользователя после завершения.

Вопрос наблюдения необязателен: личный эксперимент не должен зависеть от наличия подходящего числового поля. Приложение показывает соблюдение условия и фактические значения доступных показателей до и во время периода с числом измерений. Необычные дни не входят в средние.

Приложение не выбирает главный показатель, не задаёт порог успеха и не выносит вердикт. Завершённый эксперимент сохраняет условие, вопрос, даты, вывод пользователя и необязательное следующее решение. Он отображается в общей истории изменений рядом с событиями, итогами и решениями обзоров. Периоды экспериментов не пересекаются, потому что одна дневная отметка не должна относиться к двум разным условиям.

Выполнение условия само по себе не доказывает, что эксперимент помог.

## План если-то

План если-то нужен только после разбора периода, а не в ежедневной записи.

Хороший формат:

- если возникает конкретная ситуация или фактор;
- то я делаю одно конкретное действие;
- формулировка короткая и проверяемая.

Пример: если после 22:00 тянет читать новости, то я ставлю таймер на 10 минут и закрываю вкладку.

Плохой формат:

- обещания «быть дисциплинированнее»;
- несколько изменений сразу;
- план без конкретного триггера;
- план, который невозможно проверить через неделю.

## Правило изменения функционала

Перед добавлением новой функции нужно коротко ответить:

1. Какое решение пользователя она улучшает?
2. На каком уровне она работает: день, неделя, месяц или долгий период?
3. Как она останется необязательной или низкозатратной?
4. Где она появится в аналитике?
5. Какие выводы из неё нельзя делать?

Если на эти вопросы нет ясных ответов, функцию лучше не добавлять.

## Ручной внешний анализ

Приложение не отправляет данные в нейросеть и не содержит встроенного AI-агента. Оно может только подготовить промпт и JSON для осознанной ручной передачи внешнему сервису.

- пакет явно указывает последнюю дату фактических данных;
- пользовательские идентификаторы сопровождаются понятными подписями;
- копируемый промпт содержит человекочитаемую сводку без служебных полей, внутренних идентификаторов и JSON-разметки;
- полный lossless JSON скачивается отдельно и не дублируется внутри промпта;
- неделя может содержать подробные дневные записи, а долгий период передаёт месячные сводки вместо полного дневника;
- свободный текст и число подробных записей ограничиваются предсказуемым бюджетом; сокращение всегда обозначается, исходные данные остаются в JSON;
- передаются ограничения выборки, особые дни, прошлые обзоры и контекст эксперимента;
- промпт запрещает диагнозы, оценки личности, причинные заявления и прогнозы без данных;
- интерфейс предупреждает, что в выгрузке могут находиться личные заметки.

## Безопасность и приватность

Публичная ссылка не должна означать публичный дневник. Если включено облачное хранение:

- приложение сначала показывает вход, а не основные экраны;
- локальная IndexedDB-база загружается только после подтверждённой сессии;
- локальный кэш привязан к `user.id` и не переиспользуется между разными пользователями одного браузера;
- таблицы Supabase должны работать с Row Level Security;
- frontend может использовать только publishable/anon ключ, но не `service_role`;
- доступ к строкам в базе должен проверяться через `auth.uid()`.
- по умолчанию приложение показывает только вход. Закрытая beta-регистрация включается отдельно для каждого стенда, требует серверно проверяемое приглашение и лимит участников; frontend-флаг не считается границей доступа;
- пользователь может удалить только собственный аккаунт через серверную операцию, которая повторно проверяет его Auth-токен. Привилегированный ключ не попадает во frontend; локальные данные очищаются только после подтверждённого серверного удаления;
- recovery-сессия из письма не считается обычным входом: приложение не загружает дневник и навигацию, показывает отдельную страницу нового пароля, а после успешной смены завершает сессию и требует войти заново;
- изоляция участников через RLS не означает отсутствия административного доступа у владельца инфраструктуры; это учитывается во внутренних правилах обработки данных без технических пояснений на экране входа;
- приложение не обещает zero-knowledge или сквозное шифрование. Клиентское шифрование не добавляется частично без отдельного решения о восстановлении доступа, синхронизации и резервных копиях;
- при возвращении вкладки в фокус, восстановлении видимости или сети приложение повторно сверяет облачную копию, не создавая параллельных запросов и не перезаписывая неразрешённый конфликт.

## Тон интерфейса

Интерфейс должен быть спокойным, взрослым и фактическим:

- без морализаторства;
- без давления;
- без ложных обещаний;
- без «ты молодец» за каждую галочку;
- без драматизации плохих дней;
- без терапевтической имитации;
- с уважением к тому, что жизнь сложнее одной метрики.

## Понятность интерфейсных текстов

Тексты не должны предполагать, что пользователь разделяет личные цели, профессию или словарь владельца приложения.

- заголовок и пояснение прямо говорят, что можно записать и зачем;
- сначала используется конкретное действие или наблюдаемый факт, затем — продуктовый термин, если он действительно нужен;
- абстракции вроде «движения», «траектории», «фокуса» и «реального шага» не используются без объяснения в текущем контексте;
- примеры охватывают разные сферы жизни и не создают впечатление, что раздел предназначен только для карьеры или проекта;
- placeholders помогают заполнить поле, но не навязывают пользователю готовый ответ;
- технические названия, внутренние идентификаторы и формат хранения не показываются без пользовательской пользы;
- одна и та же сущность называется одинаково в форме, обзорах, архиве, экспорте и документации.

Проверка текста: новый пользователь без дополнительного объяснения должен понять, что произойдёт после действия и какую запись от него ожидают. Если для этого нужно знать контекст владельца, формулировку следует переписать.

Главная формула продукта: меньше шума, больше фактов, один следующий шаг.
