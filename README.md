# Траектория

Персональный offline-first трекер фактов о жизни. Обычная ежедневная запись рассчитана на заполнение менее чем за минуту.

Это рабочая PWA-версия с личным входом, локальным кэшем и облачной копией через Supabase. Её можно открыть в браузере, установить на телефон или компьютер и использовать как личный трекер действий, состояния и мешающих факторов.

## Возможности

- сон, время отбоя и подъёма, качество сна и энергия;
- автоматический расчёт времени в кровати и оценка регулярности режима;
- короткий контекст сна и состояния: что мешало или влияло на день;
- вечерние факторы вроде позднего отхода ко сну, новостей, видео, работы или тревоги;
- отметка особого дня: болезнь, поездка, перегруз, событие или восстановление;
- состояние карьерного движения;
- направление действия: внешний шаг, подготовка, поддержание, восстановление или уход в сторону;
- личное определение текущего фокуса и контакта с реальностью с сохранением исторического контекста;
- бокс, бачата и другая активность;
- питание относительно заранее заданного критерия, короткий контекст и опциональный вес;
- присутствие семьи, чтения, творчества, духовного развития и отдыха;
- свои пункты для карьеры и областей жизни, например «Отклики» или «Учёба»;
- отдельный архив важных изменений, решений и жизненных событий;
- маркеры событий на графиках и сравнение равных периодов до и после;
- отдельный список законченных результатов;
- редактирование событий архива и результатов;
- недельная тепловая карта и короткий обзор;
- проверка результата решения, принятого на прошлой неделе;
- план «если-то» для одного рычага следующей недели;
- сохраняемые недельный и месячный обзоры контекста, решений и следующего фокуса;
- напоминания в конце недели и месяца о разборе периода;
- статистика и локальный анализ за 3, 6 и 12 месяцев с учётом полноты данных;
- календарная карта качества заполнения без серий и оценок;
- общая хронология событий, решений недель и месяцев, их проверки и результатов;
- локальные подсказки для разбора недели и месяца без платного ИИ;
- экспорт промпта и JSON для GPT прямо из недели, месяца и трендов;
- автоматические наблюдения по связи сна, энергии и движения;
- сравнение повторяющихся факторов с обычными днями без них, без причинных заявлений;
- отдельная базовая статистика обычных и особых дней;
- пакет и backend endpoint для будущего ИИ-анализа недели и месяца;
- месячная динамика сна, регулярности, веса, областей жизни и результатов;
- один временный эксперимент с гипотезой, показателем и итогом;
- локальный IndexedDB-кэш, облачная копия Supabase и JSON-резервные копии;
- установка как PWA и работа без сети.

## Запуск

```bash
npm install
npm run dev
```

После запуска открой адрес, который покажет терминал (обычно `http://localhost:5173`).

Проверка:

```bash
npm test
npm run build
```

## Хранение данных

Данные сначала сохраняются в IndexedDB текущего браузера, а затем приложение пытается обновить облачную копию Supabase для текущего пользователя. IndexedDB остаётся локальным кэшем и защитой от временной недоступности сети.

Если облако недоступно, изменения остаются локально и помечаются как ожидающие синхронизации. Если локальная копия и облачная копия расходятся и приложение не может безопасно выбрать сторону, оно показывает конфликт и просит вручную выбрать действие в настройках.

JSON-экспорт в настройках остаётся ручной страховкой перед восстановлением из облака, очисткой данных или экспериментами с демо-аккаунтом.

## Веб, PWA и ИИ

План развёртывания и будущей ИИ-интеграции описан в [DEPLOYMENT_AND_AI.md](./DEPLOYMENT_AND_AI.md).

План backend, авторизации, Supabase RLS и CI/CD описан в [BACKEND_AUTH_CI.md](./BACKEND_AUTH_CI.md).

Правила развития продукта, научной осторожности и добавления новых полей описаны в [PRODUCT_RULES.md](./PRODUCT_RULES.md).

Для автоматического ИИ-анализа на хостинге нужен backend endpoint `/api/analyze` и переменная окружения `OPENAI_API_KEY`. Опционально можно задать `OPENAI_MODEL`.
